services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_DB: ShopDB
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

  

volumes:
  pgdata:
