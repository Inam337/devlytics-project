# Required at build time — API origin including the /api/v1 prefix (no trailing slash)
# CI uses https://api.example.com/api/v1; replace with your production API host.
VITE_API_BASE_URL=https://api.your-domain.com/api/v1