# Required at build time — full API origin (no trailing slash, no /api/v1 prefix)
# CI uses https://api.example.com; replace with your production API host.
VITE_API_BASE_URL=https://api.your-domain.com